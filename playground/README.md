# Playground

This directory is for deliberate scratch work.

For Phase 1, start with:

- `phase1_block_mapping_demo.py`
- `phase1_vector_add_exercise.py`
- `phase1_unary_op_exercise.py`

Rule of thumb:

- if you are learning or probing, use `playground/`
- if you want reusable library code, move it into `src/triton_learn/`
